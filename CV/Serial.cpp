#include "stdafx.h"
#include <string>

using namespace std;

//This is my header...
class Serial{

	public:
		Serial::Serial();
		int Send(std::string value);

	private:

};

Serial::Serial(){
	
}

//Returns 0 if all goes well if not return a -1
int Serial::Send(string value){
	int errorCode = 0;


	return 0;
}